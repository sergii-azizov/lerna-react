import { Fragment, PureComponent } from "react";

export default class ClearScreen extends PureComponent {
    render() {
        return (
            <Fragment>
                <h2>Clear</h2>
            </Fragment>
        );
    }
};
