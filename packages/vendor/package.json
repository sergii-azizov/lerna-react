{
  "name": "vendor",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.js",
  "scripts": {
    "build": "webpack --config ./configs/webpack.config.lib.js",
    "build:dev": "cross-env NODE_ENV=development npm run build",
    "build:prod": "cross-env NODE_ENV=production npm run build",
    "build:watch": "cross-env WATCH=true npm run build:dev"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "webpack": "^4.29.4"
  }
}
